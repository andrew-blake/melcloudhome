# Home Assistant Connection Settings
# Copy this file to .env and update with your settings

# SSH Connection (for deploying custom components)
HA_SSH_HOST=ha                          # SSH hostname or IP
HA_CONTAINER=homeassistant              # Docker container name

# API Connection (for testing integrations)
HA_URL=http://ha:8123                  # Home Assistant URL
HA_TOKEN=                               # Long-lived access token
                                        # Get from: Profile → Long-Lived Access Tokens

# Note: .env file is gitignored for security
